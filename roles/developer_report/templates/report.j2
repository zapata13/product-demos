<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/main.css">
</head>
<h1>Developer Report</h1>
<body>
<table>
    <thead>
        <tr>
            <th>Hostname</th>
            <th>Operating System</th>
            <th>PATH</th>
            <th>Kernel</th>
            <th>Python Version</th>
            <th>Package Manager</th>
            <th>System Date Time</td>
        </tr>
    </thead>
    <tbody>
{% for host in groups['web'] %}
        <tr>
            <td>{{ hostvars[host]['ansible_hostname'] }}</td>
            <td>{{ hostvars[host]['ansible_distribution'] }} {{hostvars[host]['ansible_distribution_version']}}</td>
            <td>{{ hostvars[host]['ansible_env']['PATH'] }}</td>
            <td>{{ hostvars[host]['ansible_kernel'] }}</td>
            <td>{{ hostvars[host]['ansible_python_version'] }}</td>
            <td>{{ hostvars[host]['ansible_pkg_mgr'] }}</td>
            <td>{{ hostvars[host]['ansible_date_time']['date'] }}</td>

        </tr>
{% endfor %}
    </tbody>
</table>

</body>
</html>
